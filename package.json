{
  "name": "flight-sim",
  "private": true,
  "packageManager": "npm@10.9.4",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "dependencies": {},
  "devDependencies": {
    "turbo": "^2.8.21",
    "typescript": "^6.0.2"
  }
}
